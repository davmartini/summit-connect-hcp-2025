apiVersion: hypershift.openshift.io/v1beta1
kind: NodePool
metadata:
  name: template
  namespace: clusters
spec:
  config:
  - name: custom-ntp
  - name: custom-dns
  - name: custom-nested-virt
  arch: amd64
  clusterName: 'template'
  management:
    autoRepair: false
    upgradeType: Replace
  platform:
    type: KubeVirt
    kubevirt:
      compute:
        cores: 3
        memory: 8Gi
      rootVolume:
        type: Persistent
        persistent:
          size: 32Gi
          accessModes:
            - ReadWriteMany
          storageClass: ocs-storagecluster-ceph-rbd-virtualization
          volumeMode: Block
      additionalNetworks:
      - name: default/localnet-vlan1187
      attachDefaultNetwork: false
  release:
    image: quay.io/openshift-release-dev/ocp-release:4.20.8-multi
  replicas: 2